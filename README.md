# Notification center — prototype

Design spec for the LambdaTest / TestMu AI in-app notification center: a bell in every
product's top bar and the org-scoped, cross-product panel it opens. The spec lives in
[`notification-center.md`](notification-center.md), written in the `design-context/patterns/`
house format.

Working prototype arriving by PR.
